# pi-fusion

Subagent-native multi-model deliberation for Pi.

This package is an early scaffold. The implementation will provide explicit `/fusion` commands backed by `pi-subagents`, publish status through the `fusion` status key, and keep bundled panel agents read-only by default.

## Development

```bash
npm install
npm run check
npm test
npm run pack:dry
```
